name: Build and Publish PHP-GD Image

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'Dockerfile'
    tags:
      - 'v*.*.*'
  release:
    types: [published]
  # AUTOMATISIERUNG: Einmal pro Woche (Montag 03:00 UTC)
  schedule:
    - cron: '0 3 * * 1'
  # Erlaubt es, den Build manuell im GitHub Interface zu starten
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.event_name == 'release' || github.event_name == 'schedule' }}

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          # 'pull: true' stellt sicher, dass das aktuellste Basis-Image vom DockerHub geladen wird
          pull: true
          tags: ${{ steps.meta.outputs.tags }}
:x
